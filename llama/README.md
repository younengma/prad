1. first like gpt 2, you need download the task data and converted as the format like 'gpt2/data'
2. run the training with `python finetunine.py --adapter_config_file configs/<config_name>.json  --task <task> --seed <seed> --num_epochs <epoch>  --learning_rate <lr>`
